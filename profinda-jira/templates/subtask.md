<!--
SUB-TASK = optional. Use when breaking a Story/Task into steps (recommended for
multi-session/parallel work). If you use a checklist in the parent instead, you
don't need sub-tasks. Intentionally minimal so teams/agents can use it their way.
Dependencies are Jira issue links (blocks / is blocked by), not a section here.
No estimation on sub-tasks — the parent carries the estimate.
-->

## Objective
The single step this delivers. What "done" means for this step.

## Spec / thinking
The plan and reasoning for HOW to make the change. This is where the thinking
lives before code is written — visible and resumable by anyone.

## Files / areas to touch
Modules, services, or areas affected.

## Verification
How to prove this step works (tests, manual check, command).
